apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-dev
  labels:
    app: rabbitmq
    env: development
spec:
  externalIPs: [139.59.166.134,139.59.166.152]
  ports:
  - port: 5672
    targetPort: "amqp-ssl"
    name: amqp-ssl
  - port: 15673
    targetPort: "web-management"
    name: web-management
  - port: 8884
    targetPort: "mqtt-ssl"
    name: mqtt-ssl
  selector:
    app: rabbitmq
    env: development
