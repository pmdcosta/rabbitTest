apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  labels:
    app: rabbitmq
spec:
  ports:
  - port: 5671
    name: amqp-ssl
  - port: 15672
    name: web-management
  - port: 8883
    name: mqtt-ssl
  selector:
    app: rabbitmq

